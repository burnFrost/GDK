//Physics. Alpha version.


//3D
float distance(vector<int>& A, vector<int>& B){
return sqrt(pow(A[0]-B[0], 2)+pow(A[1]-B[1], 2)+pow(A[2]-B[2], 2));
}

//float angle()

//float lookAt()

//2D

float distance2d(vector<int>& A, vector<int>& B){
return sqrt(pow(A[0]-B[0], 2)+pow(A[1]-B[1], 2));
}

//float angle2d()

//float lookAt2d()
